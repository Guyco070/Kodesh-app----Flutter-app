// Smoke test — verifies the test harness runs without errors.
// Full unit tests live in test/unit/.
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('test harness is operational', () {
    expect(1 + 1, 2);
  });
}
